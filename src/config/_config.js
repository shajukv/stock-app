export const config = {
  baseUrl: 'https://api-pub.bitfinex.com/',
  wsBaseUrl: 'wss://api-pub.bitfinex.com/',
}
export const API_URL = `${config.baseUrl}/v2/`
export const WS_API_URL = `${config.wsBaseUrl}/ws/2`

export const PageRoutes = {
  trading: 'trading'
}